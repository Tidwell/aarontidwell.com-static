<div class="link-container">
	{{#if this.github }}
		<a target= "_new" href="{{this.github}}">
			<i class="fa fa-github"></i>
			Github
		</a>
	{{/if}}
	{{#if this.url}}
		<a target="_new" href="{{this.url}}">
			<i class="fa fa-file-o"></i>
			Site
		</a>
	{{/if}}
</div>